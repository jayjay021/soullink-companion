# Database (SQLite)
# Default: file:./dev.db (places in prisma folder)
# For production, set to your desired path, e.g.: file:/var/lib/soullink/database.db
DATABASE_URL="file:./dev.db"tabase (SQLite)
# Default: file:./prisma/dev.db (automatically set if not specified)
# For production, set to your desired path, e.g.: file:/var/lib/soullink/database.db
# DATABASE_URL="file:./prisma/dev.db"

# Server
PORT=5001
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:3000
